#version 430

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(std430) buffer ssbo {
    float data[];
};

void main() {
    data[gl_GlobalInvocationID.y*700 + gl_GlobalInvocationID.x] = gl_GlobalInvocationID.y*700 + gl_GlobalInvocationID.x;
}